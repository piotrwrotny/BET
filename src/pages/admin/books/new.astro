---
import AdminLayout from "@/layouts/AdminLayout.astro";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { ServerError } from "@/components/auth/ServerError";

export const prerender = false;

if (Astro.locals.role !== "admin") {
  return Astro.redirect("/dashboard");
}

const errorParam = Astro.url.searchParams.get("error");

function safeDecodeURIComponent(value: string | null): string | null {
  if (!value) return null;
  try {
    return decodeURIComponent(value);
  } catch {
    return value;
  }
}

const errorMessage = safeDecodeURIComponent(errorParam);
---

<AdminLayout title="Nowa książka">
  <div class="mb-4">
    <a href="/admin/books" class="text-muted-foreground text-sm hover:underline">← Książki</a>
  </div>

  {errorMessage && <ServerError message={errorMessage} />}

  <form method="post" action="/api/admin/books" class="max-w-lg space-y-4">
    <div class="space-y-1">
      <Label htmlFor="title">Tytuł *</Label>
      <Input id="title" name="title" type="text" required placeholder="Tytuł książki" />
    </div>

    <div class="space-y-1">
      <Label htmlFor="cover_url">URL okładki</Label>
      <Input id="cover_url" name="cover_url" type="url" placeholder="https://example.com/cover.jpg" />
      <p class="text-muted-foreground text-xs">Opcjonalny. Wklej link do obrazu okładki.</p>
    </div>

    <div class="space-y-1">
      <Label htmlFor="description">Opis</Label>
      <Textarea id="description" name="description" rows={3} placeholder="Krótki opis książki…" />
    </div>

    <div class="flex gap-2">
      <button
        type="submit"
        class="bg-primary text-primary-foreground hover:bg-primary/90 inline-flex h-9 items-center rounded-md px-4 text-sm font-medium transition-colors"
      >
        Utwórz książkę
      </button>
      <a
        href="/admin/books"
        class="border-border text-muted-foreground hover:bg-muted inline-flex h-9 items-center rounded-md border px-4 text-sm transition-colors"
      >
        Anuluj
      </a>
    </div>
  </form>
</AdminLayout>
